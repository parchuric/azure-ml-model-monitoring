# Azure ML Model Monitoring - Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# Required: Azure subscription and workspace details
AZURE_SUBSCRIPTION_ID=your-subscription-id-here
AZURE_RESOURCE_GROUP=your-resource-group-name
AZURE_ML_WORKSPACE=your-workspace-name

# Required: Default datastore name for the workspace
DEFAULT_DATASTORE=your-datastore-name

# Required for Service Principal authentication (optional if using az login)
AZURE_CLIENT_ID=your-service-principal-client-id
AZURE_CLIENT_SECRET=your-service-principal-client-secret
AZURE_TENANT_ID=your-azure-tenant-id

# Optional: Email for monitor alerts (required by API, uses placeholder if not set)
# ALERT_EMAIL=your-email@example.com
